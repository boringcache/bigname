workspace = "boringcache/benchmarks"

[entries.cargo-registry-cache]
tag = "bigname-full-ci-registry-cache"

[entries.cargo-registry-index]
tag = "bigname-full-ci-registry-index"

[entries.cargo-git-db]
tag = "bigname-full-ci-git-db"

[entries.cargo-target]
kind = "cargo-target"
path = "target"
tag = "bigname-full-ci-target"
exclude = ["bigname-e2e-*", "api-container-shutdown"]

[entries.foundry]
path = "~/.foundry/cache"
tag = "bigname-full-ci-foundry-1.7.1"

[entries.npm-cache]
tag = "bigname-full-ci-npm"

[profiles.dependencies]
entries = ["cargo-registry-cache", "cargo-registry-index"]

[profiles.cargo]
entries = ["cargo-target", "cargo-git-db"]

[profiles.foundry]
entries = ["foundry"]

[profiles.npm]
entries = ["npm-cache"]

[adapters.cargo]
profiles = ["cargo"]
compiler-cache = "sccache"
fail-on-cache-error = true

[adapters.sccache]
tag = "bigname-full-ci-rust"

[adapters.docker]
command = ["docker", "buildx", "bake", "--file", ".github/docker-bake.hcl"]
tag = "bigname-full-ci-docker"
mount-cache = true
tool-cache = ["sccache:bigname-full-ci-docker-rust"]
fail-on-cache-error = true
